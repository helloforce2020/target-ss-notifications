package com.target.notifications.enums;

/**
 * This enum indicates the mode of channel used for notifications.
 * 
 * @author vinaymajety
 *
 */
public enum Transport {
  SMS, EMAIL, PUSH, IN_APP, WEBHOOKS, VOICE
}