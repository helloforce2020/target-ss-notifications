package com.target.notifications.enums;

/**
 * This enum indicates if the event is created for a transactional use case of batch.
 * 
 * @author vinaymajety
 *
 */
public enum EventType {
  TRANSACTIONAL, BATCH
}